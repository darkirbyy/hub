{# Simple centered spinner to use when loading content with turbo-frame #}

{% block spinner %}
  <div class="d-flex justify-content-center">
    <div class="spinner-border text-primary text-center" role="status">
      <span class="visually-hidden">{{ 'aria.loading'|trans }}</span>
    </div>
  </div>
{% endblock %}
