{% block spinner %}
  <div class="d-flex justify-content-center">
    <div class="spinner-border text-secondary text-center" role="status">
      <span class="visually-hidden">{{ 'aria.loading'|trans }}</span>
    </div>
  </div>
{% endblock %}
